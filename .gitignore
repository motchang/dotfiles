*.elc
*~
\#*\#
.DS_*
node_modules/
dist/

# Per-machine Claude Code permissions. ~/.config/git/ignore covers this too, but
# that file is generated by gibo and is not tracked here, so a fresh machine
# would otherwise stage it.
.claude/settings.local.json
